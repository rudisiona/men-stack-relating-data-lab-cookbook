<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>All Users</title>
</head>
<body>

  <%- include('../partials/_navbar.ejs') %>
    <ul>
        <% users.forEach(user => { %>
          <li>
            <a href="/users/<%= user._id %>"> <%= user.username %></li>
        <% }) %>
      </ul>
</body>
</html>